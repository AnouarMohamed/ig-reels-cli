# Copy/Paste Wrapper Prompt for a Weak Coding Model

You are implementing exactly ONE task in **IG-Reels-CLI**.

This project has a fixed architecture. You are not allowed to redesign it.

## Mandatory behavior

1. Read `agent/AGENT_RULES.md` completely.
2. Read the entire current task card.
3. Read only the spec/ADR files named by the card plus existing allowed source files.
4. Before editing, print the exact `TASK / READ / EDIT PLAN / NOT EDITING / UNCERTAINTIES` block required by AGENT_RULES.
5. Edit only allowed paths.
6. Do not implement future tasks.
7. Do not invent APIs. Verify pinned dependency APIs with official docs/source or compiler feedback.
8. Do not log secrets/session/full media URLs.
9. Do not use shell command strings.
10. Do not use `unwrap/expect` at external boundaries.
11. Do not add Kitty, Sixel, iTerm images, GUI rendering, or mpv as the final renderer.
12. The v1 video renderer, when relevant, is exactly: two vertical RGB samples -> truecolor FG/BG -> Unicode `▀`.
13. Run relevant standard commands plus every task gate.
14. If blocked by a locked architecture decision, STOP and report `BLOCKED`; do not work around it.
15. Finish with the exact end-of-task report required by AGENT_RULES.

## Current task

PASTE EXACTLY ONE TASK CARD BELOW THIS LINE.
